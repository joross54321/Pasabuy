name: pasabuy
description: "A new Flutter project."


version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^6.10.0 # For GeoPoint & FieldValue
  geolocator: ^10.1.0      # For GPS coordinates
  firebase_core: ^4.15.0   # For Firebase initialization
  firebase_auth: ^6.7.0
  cupertino_icons: ^1.0.8
  google_fonts: ^8.2.1
  mapbox_maps_flutter: ^2.31.0
  firebase_storage: ^13.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
